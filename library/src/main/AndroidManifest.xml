<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ir.myandroidapp.library">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"></uses-permission>
    <uses-permission android:name="android.permission.CALL_PHONE"></uses-permission>

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <activity android:name=".activities.AddItem"></activity>
        <activity android:name=".activities.AddPage"></activity>
        <activity android:name=".activities.ItemActivity"></activity>
        <activity android:name=".activities.SubMain"></activity>
        <activity android:name=".activities.PageActivity"></activity>
        <activity android:name=".activities.CommentList"></activity>
        <activity android:name=".activities.PostsActivity"></activity>
        <activity android:name=".activities.SearchPostsActivity"></activity>

    </application>

</manifest>